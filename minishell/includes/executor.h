#ifndef EXECUTOR_H
# define EXECUTOR_H

# include "structs.h"

void	executor(t_shell *shell);
/*
char	*get_cmd_path(char *cmd, t_env *env);
void	next_cmd(t_cmd *msh);
void	exec_cmd(t_shell *msh);
void	child_proccess(t_shell *myshell, t_cmd *mycmd);
*/
#endif